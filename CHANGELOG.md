# 更新紀錄

格式：修了什麼、**為什麼會壞**。知道根因才知道自己要不要更新。

---

## v0.1.1 — 2026-07-26

第一批使用者回饋的修正。**建議所有 v0.1.0 使用者更新**，直接安裝覆蓋即可，設定與字典會留著。

### 修正

**面板被其他視窗壓到底層後就叫不回來**
建立面板時漏了 `HWND_TOPMOST`——只設了 `WS_EX_TOPMOST` 的樣式位元，但 Z 順序從來沒被設定過。而不搶焦點的視窗（`WS_EX_NOACTIVATE`）**永遠不會因為被點擊而自然浮上來**，所以沉下去就是永久的。
現在除了正確設定，還每 2 秒重新宣告一次最上層——其他程式也會把自己設成最上層、全螢幕程式會搶走那一層，只設一次不夠。

**提示音是「滴滴嘟嘟」的機械音**
`winsound.Beep` 產生的是純方波、完全沒有包絡。改成合成正弦音＋12ms 起音／25ms 收音：開始是上行兩音、結束是下行兩音。
不想要可以在 `config.json` 把 `sound` 設成 `beep`（舊的方波）或 `off`（靜音）。

**首次啟動會默默下載幾百 MB**
安裝器內建 `base` 模型，但程式依 CPU 核心數推薦了 `small`——兩者對不上，於是第一次啟動就在背景下載 463MB，而畫面只顯示「載入中」。
現在會先看本機已經有什麼模型再決定；真的需要下載時，面板明講「⬇ 首次下載語音模型（約 N MB）」。

**提示音會被錄進音檔**
原本在開始收音之後才播放。改成先播、等 0.12 秒再開始收。

### 新增

- **面板直接標示快捷鍵**：待命時顯示「或按 Ctrl+Alt+Z」。讀的是你實際的設定，改了熱鍵面板跟著變。
- **`📋` 一鍵複製最後結果**：字沒進輸入框時不用重講一次。
- **目標視窗白話化**：`WINWORD.EXE` → `📄 Word`、`LINE.exe` → `💬 LINE`。
- **講「第一…第二…」自動分行**：只插入換行，**一個字都不改**——條列化很容易變成「幫你改寫」，而改寫就可能改掉原意。

---

## v0.1.0 — 2026-07-26

第一個公開版本。

- 本機語音轉寫（faster-whisper），音訊不出網
- 浮動小面板：目標視窗、即時音量條、右鍵取消
- 全域熱鍵走 Windows 原生 `RegisterHotKey`
- 兩層整理：本機規則（零設定）＋ 可選 LLM（本機 ollama 或雲端免費層）
- `doctor.py` 環境健檢、`tune.py` 硬體剖析與實測調校
- 一鍵安裝檔，不需要 Python

### 已知問題（v0.1.1 已修）

面板會沉到底層、提示音刺耳、首次啟動要等下載。**建議直接用 v0.1.1。**
