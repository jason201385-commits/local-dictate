# 更新紀錄

格式：修了什麼、**為什麼會壞**。知道根因才知道自己要不要更新。

---

## v0.1.4 — 2026-07-30

**建議所有 v0.1.3（含）以前的使用者更新。** 這一版修掉兩個授權問題，其中一個比較嚴重。

### 🔴 移除 FFmpeg：先前版本散布了 GPL 授權的 x264／x265

v0.1.3（含）以前的安裝檔與 portable zip 內含 `_internal/av.libs/`，那裡面除了 FFmpeg，還有 **`libx264`（2.3MB）與 `libx265`（12.6MB）——兩者都是 GPL-2.0-or-later**。從 avcodec DLL 抽出的 FFmpeg 內嵌組態字串是：

```
--enable-version3 --enable-libx264 --enable-libx265
```

**而 v0.1.3 的 THIRD_PARTY_NOTICES.md 寫的是「PyAV 官方 wheel 之 LGPL 組態 build（不含 x264/x265 等 GPL 元件）」——那句話與事實相反。** 這比漏附授權文更糟：漏附是疏忽，錯誤宣稱會誤導拿這份程式去再散布的人。當時那句話是沒有實際檢查 DLL 就寫下的。

處理方式是**直接不要散布它**：`build/local-dictate.spec` 的 `excludes` 拿掉 `av`，加一個 runtime hook（`build/rthook_no_av.py`）用 stub 頂替 faster-whisper 的 `import av`。

為什麼可以這樣做：**即時聽寫從來沒用到 FFmpeg**。麥克風音訊在 `dictate.py` 是 `np.frombuffer(...).astype(np.float32)` 直接餵給模型，不走 faster-whisper 的檔案解碼路徑；av 只有 `decode_audio()` 會用到，那是「從音檔轉錄」才需要的。實測擋掉 av 之後模型載入與轉錄完全正常。

- **對你的影響**：功能零損失，安裝檔小約 25MB。唯一的代價是打包版不能從音檔轉錄——要用請從原始碼執行（`pip install av`，那時 FFmpeg 是你自己裝的）。
- CI 加了硬斷言：產物一旦出現 `x264`／`x265`／`avcodec` 等檔名就擋下 release。

### portable zip 先前沒有附授權文件

v0.1.3 只在 `build/installer.iss` 加了授權文件。但 portable zip 是 CI 直接壓 `dist\local-dictate\*`，**完全繞過 installer.iss**。CI 全綠、乾淨機五項斷言全過、release 正常發佈——沒有任何一個環節會發現免安裝版少了東西，因為從來沒有人驗過「授權文件真的在產物裡」。是事後把已發佈的 zip 抓下來拆開才看到的。

**為什麼會壞**（這一條值得看，它是「綠燈不等於做到了」的標準案例）：
v0.1.3 只在 `build/installer.iss` 加了授權文件。但 portable zip 是 CI 直接壓 `dist\local-dictate\*`，**完全繞過 installer.iss**。CI 全綠、乾淨機五項斷言全過、release 正常發佈——沒有任何一個環節會發現免安裝版少了東西，因為從來沒有人驗過「授權文件真的在產物裡」。是事後把已發佈的 zip 抓下來拆開才看到的。

修正三件事：

1. **授權文件改由 `build/local-dictate.spec` 收進 PyInstaller 產物**。這樣任何從 `dist\` 出發的散布形式（安裝檔、portable zip、以後可能有的其他形式）都自動含有。`installer.iss` 不再重複列，單一來源。
2. **元件清單改成自動產生**（新增 `build/gen_notices.py`，CI 每次建置時從實際安裝環境重新產出）。手工維護同時出了兩種錯：漏掉被相依進來的套件（`click` 等 **25 個**，手工清單只列了 18 個、實際有 43 個），以及版本號寫成開發機的（`tqdm 4.67.3`，CI 實際裝 `4.70.0`）。兩種錯都不會有徵兆。
3. **CI 加了兩道授權斷言**：打包後檢查產物裡有 `THIRD_PARTY_NOTICES.md`／`LGPL-COMPLIANCE.md`／`LICENSE` 與 20 份以上授權原文（含 LGPL-2.1／LGPL-3.0／GPL-3.0 全文）；乾淨機驗證新增第六項斷言，確認**安裝之後**使用者機器上也有。以後再漏就會擋下 release，不會靜默出貨。

程式行為仍然零改動。

---

## v0.1.3 — 2026-07-30

**程式行為零改動。** 如果你已經在用 v0.1.2，功能上沒有任何差別，更新只是為了讓你手上那份的授權文件是完整的。

新增 `THIRD_PARTY_NOTICES.md`＋`licenses/`（18 個打包元件的授權清單與原文）與 `LGPL-COMPLIANCE.md`（pynput／FFmpeg 的 LGPL 替換說明），`installer.iss` 把它們一併裝進程式目錄；README 補第三方授權段落與「與比較產品無關聯」聲明。

**為什麼會壞**：安裝檔從第一版就打包了 MIT／BSD／Apache／**LGPL** 元件（faster-whisper、pynput、PyAV 內的 FFmpeg DLL、PortAudio…），但 release 沒附它們的授權原文。MIT／BSD 要求散布時保留著作權聲明，LGPL 還要求提供授權全文與「用你自己的版本替換」的途徑——這些跟程式跑不跑得動無關，缺了就是散布違約。

> ⚠️ **這一版的說明有兩處是錯的，v0.1.4 已更正**：
> 1. 「FFmpeg 是 PyAV 官方 wheel 的 LGPL 組態（不含 x264／x265），沒有 GPL 傳染」——**相反**。那份 wheel 內含 GPL-2.0+ 的 libx264／libx265，組態是 `--enable-version3 --enable-libx264 --enable-libx265`。這句話當時沒有實際檢查 DLL 就寫下了。
> 2. 這一版只有安裝檔附上授權文件，**portable zip 沒有**。
>
> 兩件事都在 v0.1.4 修掉。這一版的 release 資產不建議再使用。

---

## v0.1.2 — 2026-07-26

**如果你已經裝了 v0.1.1，這版只有檔名與版本號的差別，功能完全相同，可以不用更新。**

安裝器的版本號原本寫死在腳本裡，所以 v0.1.1 的 release 裡放的檔案叫
`local-dictate-setup-0.1.0.exe`，「新增或移除程式」也會顯示錯的版本。
現在版本號從 git tag 帶進去。

---

## v0.1.1 — 2026-07-26

第一批使用者回饋的修正。**建議所有 v0.1.0 使用者更新**，直接安裝覆蓋即可，設定與字典會留著。

### 修正

**面板被其他視窗壓到底層後就叫不回來**
建立面板時漏了 `HWND_TOPMOST`——只設了 `WS_EX_TOPMOST` 的樣式位元，但 Z 順序從來沒被設定過。而不搶焦點的視窗（`WS_EX_NOACTIVATE`）**永遠不會因為被點擊而自然浮上來**，所以沉下去就是永久的。
現在除了正確設定，還每 2 秒重新宣告一次最上層——其他程式也會把自己設成最上層、全螢幕程式會搶走那一層，只設一次不夠。

**提示音是「滴滴嘟嘟」的機械音**
`winsound.Beep` 產生的是純方波、完全沒有包絡。改成合成正弦音＋12ms 起音／25ms 收音：開始是上行兩音、結束是下行兩音。
不想要可以在 `config.json` 把 `sound` 設成 `beep`（舊的方波）或 `off`（靜音）。

**首次啟動會默默下載幾百 MB**
安裝器內建 `base` 模型，但程式依 CPU 核心數推薦了 `small`——兩者對不上，於是第一次啟動就在背景下載 463MB，而畫面只顯示「載入中」。
現在會先看本機已經有什麼模型再決定；真的需要下載時，面板明講「⬇ 首次下載語音模型（約 N MB）」。

**提示音會被錄進音檔**
原本在開始收音之後才播放。改成先播、等 0.12 秒再開始收。

### 新增

- **面板直接標示快捷鍵**：待命時顯示「或按 Ctrl+Alt+Z」。讀的是你實際的設定，改了熱鍵面板跟著變。
- **`📋` 一鍵複製最後結果**：字沒進輸入框時不用重講一次。
- **目標視窗白話化**：`WINWORD.EXE` → `📄 Word`、`LINE.exe` → `💬 LINE`。
- **講「第一…第二…」自動分行**：只插入換行，**一個字都不改**——條列化很容易變成「幫你改寫」，而改寫就可能改掉原意。

---

## v0.1.0 — 2026-07-26

第一個公開版本。

- 本機語音轉寫（faster-whisper），音訊不出網
- 浮動小面板：目標視窗、即時音量條、右鍵取消
- 全域熱鍵走 Windows 原生 `RegisterHotKey`
- 兩層整理：本機規則（零設定）＋ 可選 LLM（本機 ollama 或雲端免費層）
- `doctor.py` 環境健檢、`tune.py` 硬體剖析與實測調校
- 一鍵安裝檔，不需要 Python

### 已知問題（v0.1.1 已修）

面板會沉到底層、提示音刺耳、首次啟動要等下載。**建議直接用 v0.1.1。**
